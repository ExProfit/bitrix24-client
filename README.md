# Bitrix24 Client

Python клиент для работы с Bitrix24 REST API с поддержкой типизированных моделей (Pydantic).

## 🚀 Установка

```bash
pip install -e .
